{
  "servers": [
    { "server": "firecrawl", "credential": "FIRECRAWL_API_KEY", "scope": "crawl/scrape" },
    { "server": "tavily-ai", "credential": "TAVILY_API_KEY", "scope": "web search" },
    { "server": "context7", "credential": "CONTEXT7_API_KEY", "scope": "docs read-only" }
  ]
}
