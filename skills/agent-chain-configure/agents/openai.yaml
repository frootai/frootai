# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: agent-chain-configure). Safe to delete or edit.
interface:
  display_name: "Agent Chain Configure"
  short_description: "Orchestrate builder, reviewer, and tuner agent handoffs for solution plays…"
  default_prompt: "Orchestrate builder, reviewer, and tuner agent handoffs for solution plays — define roles, model routing, token budgets, and quality gates"
