# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: api-rate-limit-configure). Safe to delete or edit.
interface:
  display_name: "API Rate Limit Configure"
  short_description: "Configure API rate limiting, APIM throttling, and retry behavior for AI…"
  default_prompt: "Configure API rate limiting, APIM throttling, and retry behavior for AI endpoints — absorb bursts, isolate noisy tenants, and control 429 retries"
