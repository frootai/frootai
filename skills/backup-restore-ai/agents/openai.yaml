# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: backup-restore-ai). Safe to delete or edit.
interface:
  display_name: "Backup Restore AI"
  short_description: "Back up AI data, vector indexes, and configuration state — recover prompts…"
  default_prompt: "Back up AI data, vector indexes, and configuration state — recover prompts, conversations, and search assets after failures"
