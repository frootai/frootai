# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: circuit-breaker-add). Safe to delete or edit.
interface:
  display_name: "Circuit Breaker Add"
  short_description: "Add a circuit breaker, fallback routing, and health checks for Azure OpenAI…"
  default_prompt: "Add a circuit breaker, fallback routing, and health checks for Azure OpenAI calls — contain 429 and 503 cascades and preserve service"
