# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: fai-azure-app-config). Safe to delete or edit.
interface:
  display_name: "Azure App Config"
  short_description: "Wire Azure App Configuration with feature flag targeting filters, Key Vault…"
  default_prompt: "Wire Azure App Configuration with feature flag targeting filters, Key Vault reference chaining, .NET/Python SDK refresh polling, and blue/green rollout"
