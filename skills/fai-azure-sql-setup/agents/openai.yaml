# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: fai-azure-sql-setup). Safe to delete or edit.
interface:
  display_name: "Azure SQL Setup"
  short_description: "Provision Azure SQL Database with AAD-only auth, private networking, backup…"
  default_prompt: "Provision Azure SQL Database with AAD-only auth, private networking, backup policies, and workload performance tuning"
