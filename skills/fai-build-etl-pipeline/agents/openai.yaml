# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: fai-build-etl-pipeline). Safe to delete or edit.
interface:
  display_name: "Build ETL Pipeline"
  short_description: "Design ETL/ELT pipelines for AI data ingestion — extraction, transformation…"
  default_prompt: "Design ETL/ELT pipelines for AI data ingestion — extraction, transformation, quality checks, scheduling, and idempotent loads"
