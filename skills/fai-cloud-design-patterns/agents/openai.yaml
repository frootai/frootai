# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: fai-cloud-design-patterns). Safe to delete or edit.
interface:
  display_name: "Cloud Design Patterns"
  short_description: "Apply cloud design patterns to AI workloads — bulkhead, retry with backoff…"
  default_prompt: "Apply cloud design patterns to AI workloads — bulkhead, retry with backoff, circuit breaker, CQRS, and the saga pattern"
