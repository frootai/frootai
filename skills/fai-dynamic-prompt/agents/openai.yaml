# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: fai-dynamic-prompt). Safe to delete or edit.
interface:
  display_name: "Dynamic Prompt"
  short_description: "Build dynamic prompts that adapt based on user context and history."
  default_prompt: "Build dynamic prompts that adapt based on user context and history"
