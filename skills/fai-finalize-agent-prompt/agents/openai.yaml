# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: fai-finalize-agent-prompt). Safe to delete or edit.
interface:
  display_name: "Finalize Agent Prompt"
  short_description: "Finalize agent system prompts with guardrails and context anchoring."
  default_prompt: "Finalize agent system prompts with guardrails and context anchoring"
