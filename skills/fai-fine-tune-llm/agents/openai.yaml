# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: fai-fine-tune-llm). Safe to delete or edit.
interface:
  display_name: "Fine Tune LLM"
  short_description: "Prepare and execute LLM fine-tuning with quality evaluation."
  default_prompt: "Prepare and execute LLM fine-tuning with quality evaluation"
