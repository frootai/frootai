# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: fai-mcp-go-scaffold). Safe to delete or edit.
interface:
  display_name: "MCP Go Scaffold"
  short_description: "Scaffold Go MCP servers with the mcp-go SDK, typed tool handlers, middleware…"
  default_prompt: "Scaffold Go MCP servers with the mcp-go SDK, typed tool handlers, middleware, and production deployment"
