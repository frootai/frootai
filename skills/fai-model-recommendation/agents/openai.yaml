# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: fai-model-recommendation). Safe to delete or edit.
interface:
  display_name: "Model Recommendation"
  short_description: "Recommend optimal AI model (Claude, GPT-4o, Llama, etc.) based on latency…"
  default_prompt: "Recommend optimal AI model (Claude, GPT-4o, Llama, etc.) based on latency, cost, quality, and task-specific needs"
