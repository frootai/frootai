# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: fai-multi-stage-docker). Safe to delete or edit.
interface:
  display_name: "Multi Stage Docker"
  short_description: "Generates optimized multi-stage Dockerfiles with minimal runtime images."
  default_prompt: "Generates optimized multi-stage Dockerfiles with minimal runtime images"
