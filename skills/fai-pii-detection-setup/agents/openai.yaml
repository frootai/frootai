# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: fai-pii-detection-setup). Safe to delete or edit.
interface:
  display_name: "PII Detection Setup"
  short_description: "Set up PII detection and masking with Azure AI Language service"
  default_prompt: "Set up PII detection and masking with Azure AI Language service"
