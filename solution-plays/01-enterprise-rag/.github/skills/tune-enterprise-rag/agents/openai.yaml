# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: tune-enterprise-rag). Safe to delete or edit.
interface:
  display_name: "Enterprise RAG"
  short_description: "Tune Enterprise RAG for production — optimize chunking, search config, model…"
  default_prompt: "Tune Enterprise RAG for production — optimize chunking, search config, model routing, caching, temperature, token budget, evaluation thresholds"
