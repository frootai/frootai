# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: deploy-landing-zone). Safe to delete or edit.
interface:
  display_name: "Landing Zone"
  short_description: "Deploy AI Landing Zone to Azure — validate Bicep with AVM modules, deploy…"
  default_prompt: "Deploy AI Landing Zone to Azure — validate Bicep with AVM modules, deploy hub-spoke networking, configure private endpoints, set up RBAC, enable monitoring"
