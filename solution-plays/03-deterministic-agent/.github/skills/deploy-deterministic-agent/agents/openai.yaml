# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: deploy-deterministic-agent). Safe to delete or edit.
interface:
  display_name: "Deterministic Agent"
  short_description: "Deploy deterministic agent to Azure — configure zero-temperature model, set up…"
  default_prompt: "Deploy deterministic agent to Azure — configure zero-temperature model, set up structured output, deploy guardrails, configure abstention logic"
