# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: evaluate-deterministic-agent). Safe to delete or edit.
interface:
  display_name: "Deterministic Agent"
  short_description: "Evaluate deterministic agent quality — test reproducibility, abstention…"
  default_prompt: "Evaluate deterministic agent quality — test reproducibility, abstention accuracy, confidence calibration, guardrail coverage, latency consistency"
