# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: tune-deterministic-agent). Safe to delete or edit.
interface:
  display_name: "Deterministic Agent"
  short_description: "Tune deterministic agent — optimize confidence thresholds, abstention rates…"
  default_prompt: "Tune deterministic agent — optimize confidence thresholds, abstention rates, latency, model routing, response caching, cost"
