# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: tune-it-ticket-resolution). Safe to delete or edit.
interface:
  display_name: "It Ticket Resolution"
  short_description: "Tune IT Ticket AI — optimize classification model, routing rules, confidence…"
  default_prompt: "Tune IT Ticket AI — optimize classification model, routing rules, confidence thresholds, SLA escalation timing, KB retrieval quality"
