{
    "inputs": [
        {
            "type": "promptString",
            "id": "azure-openai-key",
            "description": "Azure OpenAI API Key (leave blank if using Managed Identity)",
            "password": true
        },
        {
            "type": "promptString",
            "id": "servicenow-api-key",
            "description": "ServiceNow API Key for ticket integration",
            "password": true
        }
    ],
    "servers": {
        "frootai": {
            "type": "stdio",
            "command": "npx",
            "args": [
                "frootai-mcp@latest"
            ],
            "envFile": "${workspaceFolder}/.env"
        }
    }
}