# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: deploy-document-intelligence). Safe to delete or edit.
interface:
  display_name: "Document Intelligence"
  short_description: "Deploy Document Intelligence — configure Azure Document Intelligence…"
  default_prompt: "Deploy Document Intelligence — configure Azure Document Intelligence, prebuilt/custom models, indexers, validation rules, PII masking"
