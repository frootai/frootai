# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: deploy-multi-agent-service). Safe to delete or edit.
interface:
  display_name: "Multi Agent Service"
  short_description: "Deploy Multi-Agent Service — configure supervisor pattern, agent topology…"
  default_prompt: "Deploy Multi-Agent Service — configure supervisor pattern, agent topology, handoff protocol, shared context store, timeout policies"
