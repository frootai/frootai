# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: tune-multi-agent-service). Safe to delete or edit.
interface:
  display_name: "Multi Agent Service"
  short_description: "Tune Multi-Agent Service — optimize routing rules, per-agent model selection…"
  default_prompt: "Tune Multi-Agent Service — optimize routing rules, per-agent model selection, timeout values, context passing, cost per task"
