# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: tune-content-moderation). Safe to delete or edit.
interface:
  display_name: "Content Moderation"
  short_description: "Tune Content Moderation — optimize severity thresholds per category, reduce…"
  default_prompt: "Tune Content Moderation — optimize severity thresholds per category, reduce false positives, custom blocklist management, latency optimization"
