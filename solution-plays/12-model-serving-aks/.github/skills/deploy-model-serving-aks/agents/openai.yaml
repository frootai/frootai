# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: deploy-model-serving-aks). Safe to delete or edit.
interface:
  display_name: "Model Serving AKS"
  short_description: "Deploy Model Serving on AKS — provision GPU node pools, deploy vLLM/TGI…"
  default_prompt: "Deploy Model Serving on AKS — provision GPU node pools, deploy vLLM/TGI containers, configure HPA, health probes, ACR, and ingress"
