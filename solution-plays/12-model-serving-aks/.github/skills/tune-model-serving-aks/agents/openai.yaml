# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: tune-model-serving-aks). Safe to delete or edit.
interface:
  display_name: "Model Serving AKS"
  short_description: "Tune Model Serving AKS — optimize GPU utilization, quantization level, batch…"
  default_prompt: "Tune Model Serving AKS — optimize GPU utilization, quantization level, batch size, spot node pools, auto-scaling rules, cost per inference"
