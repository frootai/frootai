# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: deploy-cost-optimized-ai-gateway). Safe to delete or edit.
interface:
  display_name: "Cost Optimized AI Gateway"
  short_description: "Deploy AI Gateway — configure APIM with semantic caching (Redis), token…"
  default_prompt: "Deploy AI Gateway — configure APIM with semantic caching (Redis), token budgets per tenant, multi-region load balancing, model routing policies"
