# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: deploy-multi-modal-docproc). Safe to delete or edit.
interface:
  display_name: "Multi Modal Docproc"
  short_description: "Deploy Multi-Modal DocProc — configure GPT-4o vision + Document Intelligence…"
  default_prompt: "Deploy Multi-Modal DocProc — configure GPT-4o vision + Document Intelligence OCR for images, charts, tables, stamps, handwriting"
