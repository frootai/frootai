{
    "inputs": [
        {
            "type": "promptString",
            "id": "azure-openai-key",
            "description": "Azure OpenAI API Key for GPT-4o vision (leave blank if using Managed Identity)",
            "password": true
        },
        {
            "type": "promptString",
            "id": "azure-doc-intelligence-key",
            "description": "Azure Document Intelligence API Key (leave blank if using Managed Identity)",
            "password": true
        }
    ],
    "servers": {
        "frootai": {
            "type": "stdio",
            "command": "npx",
            "args": [
                "frootai-mcp@latest"
            ],
            "envFile": "${workspaceFolder}/.env"
        }
    }
}