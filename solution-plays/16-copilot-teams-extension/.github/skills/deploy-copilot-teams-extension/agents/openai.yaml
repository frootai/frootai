# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: deploy-copilot-teams-extension). Safe to delete or edit.
interface:
  display_name: "Copilot Teams Extension"
  short_description: "Deploy Copilot Teams Extension — configure Teams app manifest, Bot Service…"
  default_prompt: "Deploy Copilot Teams Extension — configure Teams app manifest, Bot Service, SSO, Adaptive Cards, Microsoft Graph API, admin approval"
