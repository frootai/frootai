# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: evaluate-copilot-teams-extension). Safe to delete or edit.
interface:
  display_name: "Copilot Teams Extension"
  short_description: "Evaluate Copilot Teams Extension — test SSO flow, Graph API permissions…"
  default_prompt: "Evaluate Copilot Teams Extension — test SSO flow, Graph API permissions, Adaptive Card rendering, response quality, throttling resilience"
