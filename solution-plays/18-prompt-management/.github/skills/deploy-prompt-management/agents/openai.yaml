# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: deploy-prompt-management). Safe to delete or edit.
interface:
  display_name: "Prompt Management"
  short_description: "Deploy Prompt Management — set up prompt registry (Cosmos DB), versioned…"
  default_prompt: "Deploy Prompt Management — set up prompt registry (Cosmos DB), versioned templates, A/B testing framework, injection defense layer, prompt analytics"
