# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: evaluate-anomaly-detection). Safe to delete or edit.
interface:
  display_name: "Anomaly Detection"
  short_description: "Evaluate Anomaly Detection — measure precision, recall, F1 on labeled…"
  default_prompt: "Evaluate Anomaly Detection — measure precision, recall, F1 on labeled anomalies, false positive rate, detection latency, root cause accuracy"
