# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: tune-anomaly-detection). Safe to delete or edit.
interface:
  display_name: "Anomaly Detection"
  short_description: "Tune Anomaly Detection — optimize detection thresholds (σ levels), time…"
  default_prompt: "Tune Anomaly Detection — optimize detection thresholds (σ levels), time windows, suppression rules, seasonal baselines, alert routing, false positive reduction"
