# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: deploy-conversation-memory-layer). Safe to delete or edit.
interface:
  display_name: "Conversation Memory Layer"
  short_description: "Deploy Conversation Memory Layer — configure tiered memory (short-term Redis…"
  default_prompt: "Deploy Conversation Memory Layer — configure tiered memory (short-term Redis, long-term Cosmos DB, episodic vector store), compression pipeline, PII-aware"
