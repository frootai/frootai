# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: tune-conversation-memory-layer). Safe to delete or edit.
interface:
  display_name: "Conversation Memory Layer"
  short_description: "Tune Conversation Memory — optimize compression ratio, recall thresholds…"
  default_prompt: "Tune Conversation Memory — optimize compression ratio, recall thresholds, memory TTL per tier, embedding model for recall, storage cost per user"
