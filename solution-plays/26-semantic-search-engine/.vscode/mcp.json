{
    "inputs": [
        {
            "type": "promptString",
            "id": "azure-search-key",
            "description": "Azure AI Search Admin Key (leave blank if using Managed Identity)",
            "password": true
        },
        {
            "type": "promptString",
            "id": "azure-openai-key",
            "description": "Azure OpenAI API Key for embeddings (leave blank if using Managed Identity)",
            "password": true
        }
    ],
    "servers": {
        "frootai": {
            "type": "stdio",
            "command": "npx",
            "args": [
                "frootai-mcp@latest"
            ],
            "envFile": "${workspaceFolder}/.env"
        }
    }
}