# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: deploy-mcp-gateway). Safe to delete or edit.
interface:
  display_name: "MCP Gateway"
  short_description: "Deploy MCP Gateway — build MCP server with tool definitions, resource…"
  default_prompt: "Deploy MCP Gateway — build MCP server with tool definitions, resource handlers, transport (stdio/HTTP/SSE), input validation, sandboxing"
