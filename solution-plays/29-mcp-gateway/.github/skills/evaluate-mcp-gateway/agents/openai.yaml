# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: evaluate-mcp-gateway). Safe to delete or edit.
interface:
  display_name: "MCP Gateway"
  short_description: "Evaluate MCP Gateway — test tool invocation accuracy, input validation…"
  default_prompt: "Evaluate MCP Gateway — test tool invocation accuracy, input validation, transport reliability, LLM tool selection quality, error handling"
