# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: tune-mcp-gateway). Safe to delete or edit.
interface:
  display_name: "MCP Gateway"
  short_description: "Tune MCP Gateway — optimize tool descriptions for LLM accuracy, response…"
  default_prompt: "Tune MCP Gateway — optimize tool descriptions for LLM accuracy, response schemas, transport performance, caching, rate limits"
