# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: deploy-edge-ai-deployment). Safe to delete or edit.
interface:
  display_name: "Edge AI Deployment"
  short_description: "Deploy Edge AI — package models into containers, configure IoT Edge modules…"
  default_prompt: "Deploy Edge AI — package models into containers, configure IoT Edge modules, set up fleet deployment with canary rollouts, layered deployments, offline-capable"
