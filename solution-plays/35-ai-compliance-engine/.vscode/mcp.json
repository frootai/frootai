{
    "inputs": [
        {
            "type": "promptString",
            "id": "azure-openai-key",
            "description": "Azure OpenAI API Key for compliance analysis (leave blank if using Managed Identity)",
            "password": true
        },
        {
            "type": "promptString",
            "id": "compliance-db-connection",
            "description": "Database connection for compliance evidence storage",
            "password": true
        }
    ],
    "servers": {
        "frootai": {
            "type": "stdio",
            "command": "npx",
            "args": [
                "frootai-mcp@latest"
            ],
            "envFile": "${workspaceFolder}/.env"
        }
    }
}