# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: deploy-multimodal-agent). Safe to delete or edit.
interface:
  display_name: "Multimodal Agent"
  short_description: "Deploy Multimodal Agent — configure unified text+image+audio pipeline, GPT-4o…"
  default_prompt: "Deploy Multimodal Agent — configure unified text+image+audio pipeline, GPT-4o vision, modality routing, cross-modal synthesis, content safety per modality"
