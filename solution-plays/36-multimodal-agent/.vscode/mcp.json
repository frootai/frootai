{
    "inputs": [
        {
            "type": "promptString",
            "id": "azure-openai-key",
            "description": "Azure OpenAI API Key for GPT-4o vision + text (leave blank if using Managed Identity)",
            "password": true
        },
        {
            "type": "promptString",
            "id": "azure-speech-key",
            "description": "Azure Speech Service Key for audio processing (leave blank if using Managed Identity)",
            "password": true
        }
    ],
    "servers": {
        "frootai": {
            "type": "stdio",
            "command": "npx",
            "args": [
                "frootai-mcp@latest"
            ],
            "envFile": "${workspaceFolder}/.env"
        }
    }
}