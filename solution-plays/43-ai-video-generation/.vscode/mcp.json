{
    "inputs": [
        {
            "type": "promptString",
            "id": "azure-openai-key",
            "description": "Azure OpenAI API Key for video generation model",
            "password": true
        },
        {
            "type": "promptString",
            "id": "azure-content-safety-key",
            "description": "Azure Content Safety API Key for prompt + frame moderation",
            "password": true
        }
    ],
    "servers": {
        "frootai": {
            "type": "stdio",
            "command": "npx",
            "args": [
                "frootai-mcp@latest"
            ],
            "envFile": "${workspaceFolder}/.env"
        }
    }
}