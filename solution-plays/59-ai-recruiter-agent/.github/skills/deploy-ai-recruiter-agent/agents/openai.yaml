# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: deploy-ai-recruiter-agent). Safe to delete or edit.
interface:
  display_name: "AI Recruiter Agent"
  short_description: "Deploy AI Recruiter Agent — resume parsing with PII redaction, bias-aware…"
  default_prompt: "Deploy AI Recruiter Agent — resume parsing with PII redaction, bias-aware candidate scoring, job description generation with bias check, explainable factors"
