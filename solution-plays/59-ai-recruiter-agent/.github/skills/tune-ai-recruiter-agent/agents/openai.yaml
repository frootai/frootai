# Generated by scripts/generate-skill-ui-metadata.py — UI metadata for skill chips.
# Derived from SKILL.md frontmatter (name: tune-ai-recruiter-agent). Safe to delete or edit.
interface:
  display_name: "AI Recruiter Agent"
  short_description: "Tune AI Recruiter Agent — scoring weights, PII detection sensitivity, bias…"
  default_prompt: "Tune AI Recruiter Agent — scoring weights, PII detection sensitivity, bias thresholds, matching criteria, JD language rules, cost per screening"
