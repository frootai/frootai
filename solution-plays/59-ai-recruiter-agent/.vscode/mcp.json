{
    "inputs": [
        {
            "type": "promptString",
            "id": "azure-openai-key",
            "description": "Azure OpenAI API Key for candidate scoring and job description generation",
            "password": true
        },
        {
            "type": "promptString",
            "id": "azure-doc-intelligence-key",
            "description": "Azure Document Intelligence Key for resume parsing (PDF/DOCX)",
            "password": true
        }
    ],
    "servers": {
        "frootai": {
            "type": "stdio",
            "command": "npx",
            "args": [
                "frootai-mcp@latest"
            ],
            "envFile": "${workspaceFolder}/.env"
        }
    }
}