{
    "servers": {
        "frootai": {
            "type": "stdio",
            "command": "npx",
            "args": [
                "frootai-mcp@latest"
            ],
            "inputs": [
                {
                    "type": "promptString",
                    "id": "azure-openai-key",
                    "description": "Azure OpenAI API Key for climate narrative generation (leave blank if using Managed Identity)",
                    "password": true
                },
                {
                    "type": "promptString",
                    "id": "ngfs-api-key",
                    "description": "NGFS Scenario Explorer API Key for climate scenario data",
                    "password": true
                }
            ],
            "envFile": "${workspaceFolder}/.env"
        }
    }
}