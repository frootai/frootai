# AI-Powered Deployment Workflow — Public Safety Analytics
# Layer 3 — Agentic Workflow. Compiles to GitHub Actions for automated Azure deployment.

name: AI Deploy — Public Safety Analytics
on:
  push:
    branches: [main]
    paths: ['solution-plays/86-public-safety-analytics/infra/**']

permissions:
  contents: read
  id-token: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Validate Bicep
        run: az bicep build --file solution-plays/86-public-safety-analytics/infra/main.bicep
      - name: Validate Configs
        run: |
          for f in solution-plays/86-public-safety-analytics/config/*.json; do
            python3 -c "import json; json.load(open('$f'))"
          done

  deploy-staging:
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # environment: staging  # Configure in your repo Settings > Environments
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: azure/login@a457da9ea143d694b1b9c7c869ebb04ebe844ef5 # v2.3.0
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - name: Deploy to Staging
        run: |
          az deployment group create \
            --resource-group rg-public-safety-analytics-staging \
            --template-file solution-plays/86-public-safety-analytics/infra/main.bicep \
            --parameters solution-plays/86-public-safety-analytics/infra/parameters.json

  evaluate:
    needs: deploy-staging
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: pip install azure-ai-evaluation openai
      - name: Run Evaluation
        run: python solution-plays/86-public-safety-analytics/evaluation/eval.py
        env:
          AZURE_OPENAI_ENDPOINT: ${{ secrets.AZURE_OPENAI_ENDPOINT }}

  deploy-production:
    needs: evaluate
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # environment: production  # Configure in your repo Settings > Environments
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: azure/login@a457da9ea143d694b1b9c7c869ebb04ebe844ef5 # v2.3.0
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - name: Deploy to Production
        run: |
          az deployment group create \
            --resource-group rg-public-safety-analytics \
            --template-file solution-plays/86-public-safety-analytics/infra/main.bicep \
            --parameters solution-plays/86-public-safety-analytics/infra/parameters.json \
            --parameters environment=prod